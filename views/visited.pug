extends layout.pug

block content
  h1 Visited Restaurants
    each restaurant in visitedRestaurants
      div
        a(href=`/restaurants/${restaurant.id}`)= restaurant.name
      div
        img(src=restaurant.img width="100px" height="100px")
        
